apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ktortemplatedockerized-chart.fullname" . }}-test-connection"
  labels:
    {{- include "ktortemplatedockerized-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ktortemplatedockerized-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
